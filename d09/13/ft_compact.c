#include <stdio.h>

int		ft_compact(char **tab, int lenght)
{
	int i = lenght ;
	while(*tab[i])
	{
		while(!*tab[i])

	}
}

int main(void)
{
	int len = strlen(a);
	char a[] = "12345\0\0\0\0";

	printf("%d", ft_compact(&a, lenght));
	return(0);
}
